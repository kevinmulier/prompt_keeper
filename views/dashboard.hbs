<h2 class="mb-6 text-sm font-bold text-center sm:text-base md:text-xl lg:text-2xl">Welcome to your dashboard, {{name}} !
</h2>
{{#if prompts}}
<h4 class="mb-3 text-sm font-bold text-center sm:text-base md:text-xl lg:text-2xl">Your latest prompts</h4>
<table class="w-full text-xs text-center md:text-sm xl:text-base">
    <thead>
        <tr class="text-sm md:text-base lg:text-lg">
            <th class="w-5/12 px-2 py-4">Title</th>
            <th class="w-2/12 px-2 py-4">AI</th>
            <th class="w-2/12 px-2 py-4">Date</th>
            <th class="w-2/12 px-2 py-4">Status</th>
            <th class="w-1/12 px-2 py-4"></th>
        </tr>
    </thead>
    <tbody>
        {{#each prompts}}
        <tr class="text-xs border-t border-white md:text-sm lg:text-base">
            <td class="px-2 py-3"><a href="/prompts/{{_id}}">{{toUpperCase this.title}}</a></td>
            <td class="px-2 py-3"><span>{{ai}}</span></td>
            <td class="px-2 py-3">{{formatDate this.createdAt}}</td>
            <td class="px-2 py-3"><span>{{toUpperCase this.status}}</span></td>
            <td class="px-2 py-3"></td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<p class="mb-5 text-sm text-center md:text-base lg:text-lg">You have not added any prompts yet.</p>
<div class="flex justify-center">
    <button>
        <a href="/prompts/add"
            class="flex items-center justify-center px-4 py-3 text-sm font-bold text-center bg-blue-700 border-2 border-white rounded-lg shadow-xl hover:bg-blue-800 md:text-base lg:text-lg">Add
            your first prompt</i>
        </a>
    </button>
</div>
{{/if}}